#include<stdio.h>
#include<conio.h>
int main(){
	int num;
	printf("Enter value in hexadecimal format: ");
	scanf("%x",&num);
	printf("In Dexadecimal format = %x or %X",num,num);
	printf("\nIn Decimal format = %d",num);
getch();
return 0;
}

